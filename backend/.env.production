# ============================================
# BACKEND PRODUCTION ENVIRONMENT (VPS Ubuntu)
# ============================================

# Server
PORT=8080
NODE_ENV=production

# Security
JWT_SECRET=GANTI_DENGAN_SECRET_KEY_YANG_AMAN_DAN_RANDOM
API_TOKEN=GANTI_DENGAN_TOKEN_UNTUK_DEVICE_ESP32

# CORS - Allow requests dari domain/IP Anda
# Format: http://domain.com,http://another.com atau * untuk allow all (TIDAK AMAN!)
CORS_ORIGIN=*

# Database
DATABASE_URL=file:./prisma/db.sqlite

# Storage
STORAGE_DIR=./storage
STORAGE_MAX_SIZE=10485760

# WhatsApp (Baileys)
BAILEYS_DATA_DIR=./wa-session
DEFAULT_PHONE=

# Push Notifications (Web Push VAPID)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:admin@smartparcel.com

# MQTT Configuration
MQTT_ENABLED=true
MQTT_HOST=13.213.57.228
MQTT_PORT=1883
MQTT_USER=smartbox
MQTT_PASS=engganngodinginginmcu

# ============================================
# INSTRUCTIONS:
# ============================================
# 1. Generate JWT_SECRET yang aman:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 2. Generate API_TOKEN untuk ESP32:
#    node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
#
# 3. Generate VAPID keys untuk push notifications:
#    npx web-push generate-vapid-keys
#
# 4. Update CORS_ORIGIN jika punya domain:
#    CORS_ORIGIN=https://smartparcel.com,https://www.smartparcel.com
#
# 5. Pastikan MQTT_HOST sesuai dengan VPS IP Anda
#
# 6. Copy file ini ke VPS: /var/www/smartparcel/backend/.env
