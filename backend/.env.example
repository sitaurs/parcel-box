# Server
PORT=8080
NODE_ENV=development

# API Base URL (for Swagger docs and external references)
API_BASE_URL=http://localhost:8080/api/v1

# Database
DATABASE_URL=file:./db.sqlite

# Storage
STORAGE_DIR=./storage

# Security
JWT_SECRET=supersecret_change_this_in_production
API_TOKEN=device_token_change_this
DEFAULT_PIN=432432

# VAPID Keys for Web Push (generate with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=admin@example.com

# WhatsApp Backend Service URL (for package notifications)
WA_API_URL=http://localhost:3001

# Baileys WhatsApp (for backend-whatsapp service)
BAILEYS_DATA_DIR=./wa-session
DEFAULT_PHONE=628123456789

# MQTT Broker (ESP32-CAM Communication)
MQTT_ENABLED=true
MQTT_HOST=13.213.57.228
MQTT_PORT=1883
MQTT_USER=smartbox
MQTT_PASS=engganngodinginginmcu

# CORS (use * untuk allow semua network, atau spesifik IP untuk security)
CORS_ORIGIN=*
